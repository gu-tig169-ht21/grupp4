import 'package:flutter/material.dart';

class ColorTheme {
  static Color a = const Color.fromRGBO(217, 178, 80, 1); // gul
  static Color b = const Color.fromRGBO(80, 119, 217, 1); // kontrastfärg, blå
  static Color c =
      const Color.fromRGBO(217, 110, 80, 1); // extrafärg, orange/brun
  static Color d = const Color.fromRGBO(187, 217, 80, 1); // extrafärg, grön
  static Color e = const Color.fromRGBO(119, 217, 80, 1); // extrafärg, klargrön
  static Color? f = Colors.yellow[200]; // navbar
}
